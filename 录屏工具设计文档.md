# 屏刻 ScreenRec · 录屏工具（设计 / 使用 / 构建文档）

对标 Xbox Game Bar 的 Windows 桌面录屏工具：**手动录屏** + **记忆回放（自动后台缓冲，随时保存最近片段）**。
底层用 Chromium 桌面流（DXGI）+ WebCodecs，**不用 gdigrab**，因此不会造成系统鼠标闪烁；采集、编码、写盘均为低占用设计。

---

## 一、功能总览

| 功能 | 说明 |
| --- | --- |
| 手动录屏 | 点「录屏」→ 选择**整个屏幕或某个窗口** → 立即开录 |
| 录制悬浮条 | 录制中窗口自动收起为迷你条：**暂停 / 结束并保存**，并实时显示 `时长 · 文件大小` |
| 记忆回放 | 打开软件即自动后台缓冲最近 N 分钟（1/3/5 分钟可选）；「保存片段」= 把最近 N 分钟导出 MP4 |
| 系统声音 / 麦克风 | 记忆回放默认同时录麦克风；若 Windows 已开启「立体声混音」还自动混入系统声音（导出时合成） |
| 鼠标指针录制 | 可开关（默认开），录屏/回放画面都含鼠标箭头；关闭则不画鼠标、更省资源 |
| 开机自启动 | 设置开启后登录自动后台运行（隐藏、仅托盘）并自动开始记忆回放 |
| 全局快捷键 | `Ctrl+Alt+G` 唤出/隐藏面板；`Ctrl+Alt+R` 开/停手动录屏；`Ctrl+Alt+P` 暂停/继续 |

## 二、使用说明

1. 双击启动（开发态：`启动录屏工具.bat`；交付态：`ScreenRec-x.y.z-portable.exe`）。
2. 顶部中央出现"捕获"小面板；打开软件 2 秒后记忆回放自动缓冲（状态栏显示 `缓冲 xx / 03:00`）。
3. 手动录屏：点「录屏」→ 选屏幕/窗口 → 开始录制；面板收起为迷你条；录完点红色「结束」自动保存到视频目录并给出提示。
4. 保存记忆片段：记忆回放运行时点蓝色「保存片段」→ 最近 N 分钟导出为 MP4。
5. 设置窗口（点「设置」或托盘菜单）：
   - **保存**：视频存放位置
   - **手动录制**：分辨率（跟随屏幕/4K/2K/1080P/720P）、帧率、麦克风说明、**录制鼠标指针**
   - **记忆回放**：保留时长（1/3/5 分钟）、画质、帧率、**系统声音**（需开启立体声混音）
   - **常规**：开机自动启动
6. 托盘：显示面板 / 保存最近片段 / 停止·开启记忆回放 / 开机自启开关 / 设置 / 退出。

### 系统声音（立体声混音）开启方法
- 右键任务栏喇叭 → 声音设置 → 更多声音设置 →「录制」页 → 空白处右键勾选"显示禁用的设备" → 启用「立体声混音」并设为默认。未开启时记忆回放自动只录麦克风。

## 三、目录结构（精简后）

```
录屏/
├─ electron/              主进程（菜单/托盘/窗口/记忆回放控制/光标读取）
│  ├─ main.js             应用主入口与状态机
│  ├─ ipc.js              录制临时文件 / 保存目录等 IPC
│  ├─ preload.js          渲染层安全桥
│  ├─ cursor.js           koffi 读取系统光标坐标
│  └─ engine/replay.js    记忆回放引擎控制器
├─ src/                   渲染层（Vue 小面板 + 后台采集页）
│  ├─ App.vue             捕获面板 UI（含录制迷你条、选源、麦克风多选）
│  ├─ bgCapture.js        后台记忆回放引擎（DXGI 采集→内存环形缓冲）
│  ├─ lib/                录制内核、音频混音、光标状态等
│  └─ composables/useRecorder.js  手动录制编排
├─ capture.html           后台采集页入口（Vite 多页）
├─ index.html / settings.html / vite.config.mjs
├─ bin/ffmpeg.exe         系统声旁路与工具链（~80MB，必须保留）
└─ electron-builder.yml   便携单 exe 打包配置
```

## 四、开发与构建

```bash
npm install            # 安装依赖（含 koffi、mp4-muxer 等）
npm run build          # 构建渲染层 + 主进程（输出 dist/、dist-electron/）
npm run start          # 本地运行（或双击 启动录屏工具.bat）
npm run dist:portable  # 打包单文件便携版 → release/ScreenRec-<版本>-portable.exe
```

**交付物**：`release/ScreenRec-x.y.z-portable.exe`，双击即用、无需安装/联网。
> 该 exe 是便携自解压格式，首次运行会在临时目录解压运行时；用户数据（设置）仍写 `%APPDATA%\屏刻 ScreenRec`。

## 五、关键实现说明

- **为什么不闪鼠标**：系统声音/画面后台录制走 DXGI（Chromium 桌面流），与 ffmpeg `gdigrab` 不同，它不碰 GDI 光标 → 鼠标不再闪烁。
- **记忆回放无磁盘碎片**：编码后的 H.264 帧存内存环形缓冲（最近 N 秒），点击保存时才合成 MP4；不产生磁盘分段。
- **MP4 完整性**：mp4-muxer 会回写文件头（随机位置写），主进程写盘按 `offset` 支持随机写，保证 moov 索引完整、成片可播放。
- **DPI 缩放**：Windows 125%/150% 下按物理像素采集/编码（例如 1920×1080），避免只录到左上角。
- **后台内存参考**：UI+后台合计约 150MB 起；记忆回放画质/帧率/时长越高，环形缓冲内存越大（设置里可调，建议 12fps/3 分钟）。

## 六、常见问题

| 现象 | 处理 |
| --- | --- |
| 记忆回放保存的片段没有系统声音 | 到 Windows「录制」设备里启用「立体声混音」 |
| 鼠标在录像里没出现 | 设置里打开「录制鼠标指针」；多显示器录副屏时箭头位置可能偏移 |
| 鼠标闪烁 | 当前版本已用 DXGI 引擎，若仍偶发，托盘停止记忆回放对比确认 |
| 录出的视频播放器打不开 | 升级后旧文件作废；新版采用 offset 写盘保证 moov 完整 |
